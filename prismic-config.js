import Prismic from '@prismicio/client';

export const apiEndpoint = 'https://chadmuro-portfolio.cdn.prismic.io/api/v2';

export const accessToken = '';

export const Client = Prismic.client(apiEndpoint);
